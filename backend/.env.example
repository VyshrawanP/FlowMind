# Database URL (PostgreSQL)
DATABASE_URL="postgresql://username:password@localhost:5432/flowmind?schema=public"

# Redis URL (Optional, fallback to in-memory socket adapter if omitted)
REDIS_URL="redis://localhost:6379"

# Server Port
PORT=3001

# Groq API Key (For AI PM sprint audits and complexity suggestions)
GROQ_API_KEY=""

# GitHub Access Token (Optional, to import private repos or increase API limits)
GITHUB_TOKEN=""
